# TODO

## Castle & Placement
- Replace the temporary "spawn at player location" castle flow with a proper placement experience.
- Swap the placeholder NPC castle asset with a true castle model + interaction collider.
- Add ownership transfer and multi-castle support.
- Add real castle attack/scouting flow, including target validation, battle entry, cooldowns, and readable defender feedback.
- Add friend/trust access management for castle interaction options.
- Add guild ownership/access permissions and guild role enforcement before exposing guild castle actions.
- Persist tiered troop counts so Might can calculate tier 1 = 1, tier 2 = 2, and higher tiers without relying on the current tier-1 aggregate fallback.

## Interiors
- Move interiors to dedicated interior server instances instead of same-process coordinates.
- Add interior generation templates and upgradeable layouts.
- Add interior construction stations and visual upgrades.
- Replace the placeholder worker portal with real task-copy pathfinding, portal effects, and cross-world worker task state.
- Re-enable entity-backed interior tour labels and stationary worker/NPC anchors after the Hytale hologram/nameplate path is validated without world-thread crashes.
- Add worker-specific right-click UIs with job assignment, task dispatch, and debug controls instead of routing to the broader citizens/troops pages.

## Population & Jobs
- Implement per-citizen simulation with individual stats feeding aggregated medians.
- Build full job loops for gatherer, hunter, cook, miner, blacksmith, architect, grunt builder, trainee, and soldier roles.
- Add job assignment UI and persistent job metadata.
- Add production-safe worker task copies that leave the stationary interior anchor intact while visual jobs run in the interior or main world.

## Citizen Aging
- Finalize the real-world aging cadence ("1 Real world da..." pending).
- Add lifecycle milestones and retirement handling.
- Promote the new control-plane `AgingTickPolicy` from in-memory test/local storage to durable persistence once clock migrations are added.

## Resources & Economy
- Add production buildings for Food/Wood/Iron generation.
- Add storage limits, decay, and transport logic.
- Add pillage cooldowns, node defense risk, and richer reward tables once combat pressure exists.
- Replace safe single-block node silhouettes with verified Hytale static assets/prefabs after asset keys are stable.

## UI & UX
- Replace placeholder UI visuals with final art and flow.

## Persistence & Infrastructure
- Add migrations for future population metadata and per-citizen tables.
- Add live Redis hot-tier promotion if the process must move from memory-only fallback to Redis without a runtime restart; expected integration point is `SemanticCacheFactory` plus the Tavall semantic-cache builder once it exposes tier replacement hooks.
- Upgrade the Hytale bot harness to authenticate and hydrate shipped asset packs the same way as the real client before relying on it for local UI coverage.

## Troop Healing Follow-ups
- Exact healing balance numbers remain: current recipe durations, resource counts, facility speed modifiers, and resource-cost modifiers are deterministic but first-pass values; tune them with live combat data in `com.tavall.hytale.resourcegame.middleware.healing` (`TroopHealingRecipeSelectionHandler`, `HealingFacilityDefinitionRegistry`, `HealingFacilityModifierCalculationHandler`).
- Actual platform art files remain: global asset IDs exist for five gems, four healing items, crafting resources, and 30 healing facility levels, but Minecraft resource-pack models, Hytale assets/prefabs, Roblox asset IDs, and Discord emoji/icon references need production art; integration point is `HealingGlobalAssetBootstrapHandler`, `PlatformAssetVersionRegistrationHandler`, and platform asset manifests.
- Resource node distribution balancing remains: crafting resource families are mapped to older-era parent node families, but spawn rates/yield tables are not balanced; integration point is `MedicalCraftingResource`, `ResourceNodeCreationHandler`, and future node loot/yield handlers under `com.tavall.hytale.resourcegame.middleware.node`.
- Minecraft healing command bridge remains: middleware projections and handlers exist, but live `/troop healing ...`, `/resource give ...`, and `/facility set-healing-level ...` commands need a Minecraft adapter module because this repo has no first-party Minecraft plugin module; expected integration point is future Minecraft command classes that call `TroopWoundAssignmentHandler`, `TroopHealingStartHandler`, and `MinecraftTroopHealingProjectionHandler`.
- Hytale healing bot scenario remains: middleware projections are covered by Java tests and the live Hytale harness can start from `C:\Users\TJ\Documents\HyTaleDevServer\start.bat`, but an end-to-end script that triggers a Hytale healing UI action still needs runtime command/UI wiring; expected script path is `scripts/hytale-troop-healing-flow.mjs` calling Hytale frontend code backed by `HytaleTroopHealingProjectionHandler`.
- Roblox runtime bridge remains: in-memory binding/projection tests exist, but Roblox Studio/server forwarding of healing action IDs is not present in this repo; integration point is `RobloxTroopHealingProjectionHandler` plus a future Roblox server adapter that posts `action.healing.*` IDs to middleware.
- Discord live bot bridge remains: the Java bot runtime, slash-command registration, HTTP control ingress client, and user/admin lane permission checks now exist, but no live Discord token or Project Novus guild id was present in local or remote env files. Expected integration point: fill `/opt/tavall-resource-game/resource-game-discord.env`, start `resource-game-discord-bot`, then extend `DiscordTroopHealingProjectionHandler` with button/select-menu handlers for healing summaries.
- Future modernization resources remain TODO-only: oil, petroleum, gasoline, plastic, synthetic drugs, electricity, and modern hospital tech are intentionally absent from this older-era pass; integration point is a future era/progression policy outside current healing recipes.
- Future wound expansion remains: burn wounds and additional status injuries are not included in this pass; integration point is `WoundType`, `TroopHealingRecipeSelectionHandler`, projection tests, and balance policy after current wound set is validated.

## Control Plane Follow-ups
- Durable Kingdom Clock persistence remains: `KingdomClockControlSystem` has in-memory repository ports for `KingdomClockState`, `KingdomClockConfig`, `KingdomScheduleRule`, `AgingTickPolicy`, and last aging tick state, but no production tables/JDBC adapters yet. Expected integration point: `com.tavall.hytale.resourcegame.middleware.clock.KingdomClockRepository` plus a future clock migration.
- Full clock downstream integrations remain: citizen AI, shops, interiors, troop training, building productivity, morale, companion recovery, raid windows, seasons, holidays/festivals, laws, and tax schedules should consume the canonical clock/schedule hooks instead of adding local time logic.
- Hytale live clock bot verification now has `scripts/run-remote-control-plane-clock-flow.ps1` and `scripts/remote-control-plane-clock-flow.mjs`, which mirror `C:\Users\TJ\Documents\HytaleDevServer` to the remote 1:1 and run the player-like bot beside it. Keep this in the remote full suite and expand it when Hytale projection visuals consume clock snapshots directly.
- Minecraft live clock command verification remains: `/kingdom clock` or equivalent Minecraft adapter commands should read `KingdomClockProjection` and render phase/hour from the backend. Expected integration point: future Minecraft command adapter calling `REFRESH_KINGDOM_CLOCK_PROJECTION`.
- Roblox/Discord live clock bridge remains: in-memory projection tests exist, but live Roblox RemoteEvent and Discord summary/button surfaces still need runtime transport wiring. Expected integration point: `RobloxProjectionHandler`, `DiscordProjectionHandler`, and frontend command-envelope adapters.
- Optional Spring clock panel production hardening remains: `/control/clock` uses direct Java runtime/query and dispatcher paths now, but authentication/session-backed operator identity should replace the local owner dev operator before public exposure.
- Durable universal kingdom persistence remains: the new kingdom simulation has repository ports and in-memory adapters for tests/local control-server boot, but no Postgres tables or JDBC repositories have been added yet. Expected integration point: `com.tavall.hytale.resourcegame.middleware.kingdom.UniversalKingdomSimulationSystem.UniversalKingdomRepository` plus a future `schema/postgres/005_universal_kingdom_simulation.sql`.
- Advanced kingdom border geometry remains: rectangular borders are implemented first; polygon/custom borders need geometry validation, overlap policy, and editor support. Expected integration point: `KingdomBorderDefinition`, `KingdomBorderShape`, and `KingdomBorderContainmentHandler`-style extraction under `middleware.kingdom`.
- Production instance health checks remain: routing profiles and platform instance health commands exist, but no live polling worker updates instance health from Minecraft/Hytale/Roblox servers. Expected integration point: `PlatformInstanceHealthUpdateHandler`, `PlatformInstanceSelectionHandler`, and the control-server scheduler loop.
- Live Roblox teleport integration remains: Roblox coordinate conversion is explicit and covered in memory, but live TeleportService/place/server routing is not wired. Expected integration point: Roblox server adapter plus `REQUEST_INSTANCE_SWITCH`, `CONFIRM_INSTANCE_SWITCH`, and `FAIL_INSTANCE_SWITCH`.
- Hytale border-crossing bot verification remains: backend command and in-memory routing tests exist, but the Hytale bot harness still needs a live movement scenario that sends player location updates and observes the switch request. Expected integration point: `scripts/` bot flow plus `HytaleControlPlaneCommandBridge`.
- Minecraft live server-switch verification remains: backend fanout can issue switch intents, but a first-party Minecraft proxy/server transfer adapter is still needed. Expected integration point: `MinecraftCommandFanoutHandler`, `PlatformFrontendAdapter`, and future Minecraft plugin listener/command classes.
- Discord kingdom projection interaction remains: Discord can consume summaries/projections, but live slash/button views for kingdom dashboards, transitions, and routing alerts remain TODO. Expected integration point: `DiscordProjectionHandler`, `DiscordControlPlaneCommandBridge`, and command IDs under `ResourceGameFrontendActionCatalog`.
- Kingdom migration/archival remains partial: archive command marks backend state as archived, but migration/draining policy and player evacuation flows are not implemented. Expected integration point: `ARCHIVE_KINGDOM`, `KINGDOM_MIGRATION`, and future migration handlers under `middleware.kingdom`.
- Balancing thresholds remain first-pass: fullness, power, new-player safety, and scaling policy values are deterministic defaults but not tuned. Expected integration point: editable parameters `maxActivePlayers`, `maxRegisteredPlayers`, `maxTopGuildPower`, `maxAveragePlayerPower`, and future live analytics.
- Full control-server domain extraction remains: `tavall-resource-game-control-server` now owns the runtime entrypoint and `FrontendCommandIngressHandler`, but the underlying middleware packages still live in the Hytale resource-game artifact so active Hytale UI/bot work was not rewritten in this pass. Expected integration point: move `com.tavall.hytale.resourcegame.middleware.*` and `com.tavall.hytale.resourcegame.controlserver.*` into `tavall-resource-game-control-server`, then make Hytale/Minecraft/Roblox/Discord modules depend on contracts plus the control ingress client boundary only.
- Full Hytale runtime package migration remains: Hytale `/kd`, UI, entity/event bridge, resources, and bot-facing verification hooks still have active uncommitted work in the root plugin tree, while the new Hytale module contains the command-envelope adapter surface. Expected integration point: move `ResourceGamePlugin`, `commands/KingdomCommand*`, `ui`, Hytale services, and Hytale resources into `tavall-resource-game-hytale-frontend` after the current UI/farmstead work is committed.
- Minecraft runtime adapter implementation remains: `tavall-resource-game-minecraft-frontend` now has a command-envelope adapter surface, but a first-party Minecraft plugin command/entity/inventory bridge still needs to be implemented. Expected integration point: Minecraft command classes should create `FrontendCommandEnvelope` instances and send them to `FrontendCommandIngressHandler`.
- Roblox live ingress client remains: the Java Roblox module defines envelope factories for RemoteEvent inputs, but live Roblox Luau forwarding still needs a runtime transport client. Expected integration point: Roblox server scripts should forward shared command-envelope fields to the control server.
- Production web-control authentication remains: the Spring MVC control panel currently boots with a local owner operator for development; wire authenticated operator identity and session handling through the existing universal identity/2FA packages before exposing it beyond local/dev. Expected integration point: `com.tavall.hytale.resourcegame.controlserver.web.ControlServerConfiguration`, `ControlOperatorRepository`, and `HighRiskActionChallengeHandler`.
- Live Minecraft control hooks remain: the command pipeline fans out through adapter ports and the Minecraft protocol harness verifies connectivity, but a first-party Minecraft plugin module is still needed to receive projection refresh/control events and render troop-healing debug output. Expected integration point: `MinecraftCommandFanoutHandler`, `PlatformFrontendAdapter`, and future Minecraft command/listener classes.
- Live Hytale control hooks remain: Hytale server and bot harness verification runs from `C:\Users\TJ\Documents\HyTaleDevServer\start.bat`, but runtime UI/command wiring for control-plane wound/healing commands still needs an adapter that consumes middleware projections. Expected integration point: `HytaleCommandFanoutHandler`, `KingdomCommand`, and `HytaleTroopHealingProjectionHandler`.
- Live Discord bot deployment remains: `ResourceGameDiscordBotApplication` can run the JDA bot and submit slash commands to `/api/frontend/commands`, but no Discord token/guild id was available and Discord guild installation still requires the Developer Portal OAuth install flow. Expected integration point: `DISCORD_BOT_SYSTEM.md`, `scripts/deploy-discord-bot-remote.ps1`, `DiscordCommandFanoutHandler`, Discord button adapters, and `DiscordTroopHealingProjectionHandler`.
- Live Roblox runtime wiring remains: in-memory fanout verifies Roblox command results, but no Roblox Studio/server bridge is present. Expected integration point: `RobloxCommandFanoutHandler`, Roblox server event adapter, and `RobloxTroopHealingProjectionHandler`.
- Platform retry execution worker remains: failed fanout now records durable retry records and exposes due retries, but no background worker is registered in the live runtime yet. Expected integration point: `ControlPlatformFanoutRetryHandler`, `PostgresControlPlatformFanoutRetryRepository`, and the future control-server scheduler loop.
- Command scheduling runtime loop remains: scheduled command repositories and dispatch handler exist, but no hosted recurring worker is started by default. Expected integration point: `ControlCommandSchedulingHandler` and Spring/Hytale runtime bootstrap.
- Command-specific compensation rules remain: compensation decisions now flag partial/failed commands for manual review, but destructive future commands need specific compensation handlers once those commands exist. Expected integration point: `ControlCommandCompensationHandler` plus future command-specific policy classes.
- Citizen control-plane follow-up: canonical `CitizenData`, aging, job eligibility using the existing `CitizenJobType`, training/troop conversion, summary cache fallback, Postgres schema, commands, and projections now exist under `middleware.citizen`; live Hytale aggregate `/kd citizens add|set` still needs a deliberate bridge to canonical records after current UI/farmstead work is clean. See `CITIZEN_SYSTEM.md`.
- Citizen web-panel follow-up: optional Spring citizen dashboard/list/detail/cache pages are not added yet; expected integration point is direct Java runtime/query handlers plus `WebControlCommandSubmissionHandler`, with no internal HTTP loopback and no Spring-owned citizen state.
- Citizen cache follow-up: deterministic memory and Redis-like cache layers are implemented for tests; production Redis summary adapter should reuse `SemanticCacheFactory` conventions.
- Citizen frontend-contract follow-up: the shared contracts source now defines citizen object/action constants, but the root plugin still compiles against the currently installed shared-contracts artifact; install/update that artifact before platform projection wrappers reference the new citizen action constants directly.

## Cross-Platform Middleware Follow-ups
- External auth provider wiring remains: connect Google/Microsoft OIDC verification because this pass added `ExternalAuthProviderVerifier`/`ProviderAuthLinkHandler` abstractions but no provider secrets or OIDC client configuration are present; integration point is `com.tavall.hytale.resourcegame.middleware.identity` (`ExternalAuthProviderVerifier`, `VerifiedAuthIdentity`, `ProviderAuthLinkHandler`).
- Email delivery remains: send passwordless email codes through production email infrastructure because this repo has no mail provider; integration point is `PasswordlessEmailAuthHandler.requestSignInCode` and likely package `com.tavall.hytale.resourcegame.middleware.identity`.
- Secret encryption storage remains: replace `IsolatedSecretCodec` with a production KMS/envelope-encryption implementation because no repo crypto utility exists yet; integration point is `com.tavall.hytale.resourcegame.middleware.security.ProtectedSecretCodec` and `TwoFactorEnrollmentHandler`.
- Live PostgreSQL repositories remain for gameplay domains: `004_cross_platform_middleware.sql` defines canonical tables and control-plane JDBC repositories now exist, but identity/security/asset/guild/castle/node/economy/petition/troop/trade default tests still use in-memory ports because this repo has no Testcontainers/Failsafe setup; integration point is repository ports under `middleware.identity`, `middleware.security`, `middleware.asset`, `middleware.guild`, `middleware.castle`, `middleware.node`, `middleware.economy`, `middleware.petition`, `middleware.troop`, and `middleware.trade`.
- Runtime DI wiring remains: register the middleware backbone in `ResourceGameDependencyModule` after the exact Hytale command/UI bridge is chosen; integration point is `com.tavall.hytale.resourcegame.dependency.modules.ResourceGameDependencyModule` plus the new `middleware` handlers.
- Minecraft live integration remains: no repo-local Minecraft server setup exists and TCP 25565 was not listening during inspection; integration point is the adjacent `tavall-java-game-tools/minecraft-bot` harness plus future Minecraft adapter classes around `MinecraftProjectionHandler` and `MinecraftPlatformAccountBindingHandler`.
- Hytale live middleware command flow remains: local Hytale tooling exists, but this pass kept existing `/kd` Hytale UI/session code untouched to avoid overwriting active uncommitted work; integration point is `KingdomCommand`, `KingdomNodeCommandSupport`, `KingdomPlacementCommandSupport`, and `HytaleProjectionHandler`.
- Roblox runtime integration remains: middleware account binding/projection flows exist, but no Roblox Studio/server tooling exists in this repo; integration point is `RobloxPlatformAccountBindingHandler`, `RobloxProjectionHandler`, and a future Roblox server adapter that forwards action IDs to middleware handlers.
- Discord live bot integration remains: middleware account binding/projection flows exist, but no Discord bot token/runtime scaffolding exists in this repo; integration point is `DiscordPlatformAccountBindingHandler`, `DiscordProjectionHandler`, and a future Discord slash-command/button adapter.
- Full siege combat simulator remains: troops and war declarations are scaffolded, but combat math and siege resolution are not implemented to avoid unsafe instant outcomes; integration point is `TroopStatusTransitionHandler`, `GuildWarDeclarationHandler`, and future siege handlers under `com.tavall.hytale.resourcegame.middleware.troop`.
- Full manual vs auto battle remains: battle mode policy is not modeled yet because no combat simulator exists; integration point is future troop/war policy classes under `middleware.troop`.
- Final overthrow balancing remains: `GuildOverthrowFlowHandler` intentionally prevents instant leader removal unless explicitly enabled by policy; integration point is `com.tavall.hytale.resourcegame.middleware.petition.GuildOverthrowFlowHandler`.
- Richer first-join castle placement remains: canonical castle creation exists but the player-facing first-join placement flow is still the existing placeholder path; integration point is `CastlePlacementHandler`, `KingdomPlacementCommandSupport`, and `TODO-FIRST-JOINERS.md`.
- Premium/entitlement trade-route policy remains: no bypass behavior was added to core route math; integration point is a future entitlement policy outside `TradeRouteProgressTickHandler`.
- Production 2FA recovery hardening remains: backup code hashing and one-time use exist, but regeneration policy, user notifications, and recovery audit events need production rules; integration point is `TwoFactorRecoveryCodeHandler` and `HighRiskActionChallengeHandler`.
- Production deployment configuration remains: environment variables for auth provider credentials, email delivery, secret encryption, and live Postgres middleware repositories need deployment docs once adapters are implemented; integration point is `EnvironmentConfig`, `DatabaseConfig`, and the new middleware repository implementations.
