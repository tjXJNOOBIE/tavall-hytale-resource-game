# PC App Module

Future C# desktop/gameplay shell.

The PC app should consume middleware APIs/projections and must not own canonical gameplay state.
Its Maven module tracks the Java shared-contract artifact version for future C# contract generation, but the C# app should use generated/native equivalents rather than Java jars directly.
