<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.tavall.hytale</groupId>
        <artifactId>tavall-hytale-resource-game</artifactId>
        <version>0.1.1-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <groupId>com.tavall</groupId>
    <artifactId>pc-app</artifactId>
    <packaging>pom</packaging>

    <name>Tavall Resource Game PC App</name>
    <description>Future C# desktop/gameplay shell for the cross-platform resource game.</description>

    <properties>
        <resource.game.contract.groupId>com.tavall</resource.game.contract.groupId>
        <resource.game.contract.artifactId>shared-contracts</resource.game.contract.artifactId>
        <resource.game.contract.version>${project.version}</resource.game.contract.version>
        <resource.game.future.csharp.contract.namespace>Tavall.ResourceGame.Contracts</resource.game.future.csharp.contract.namespace>
    </properties>
</project>
